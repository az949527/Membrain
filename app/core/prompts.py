"""
Prompt 统一管理模块
3 层结构：角色定义 / 能力边界 / 行为约束
"""

# =============================================================================
# 第 1 层：角色定义
#   定义 Agent 的身份和基本行为模式
# =============================================================================

SYSTEM_PROMPT_CHAT = "你是一个智能助手，请用中文回答问题。"

REASONING_SYSTEM_PROMPT = """你是一个智能助手。你可以使用以下工具来获取信息：
- rag_search: 从本地知识库搜索用户上传的文档内容
- graph_query: 查询知识图谱中的实体关系
- web_search: 搜索网络实时信息

根据当前信息和问题，决定下一步：
1. 如果已有足够信息可以回答 → 选择 __answer__
2. 如果需要更多信息 → 选择合适的工具"""


# =============================================================================
# 第 2 层：能力边界
#   定义 Agent 能做什么、不能做什么
#   （预留：后续 Guardrails / Memory 相关 prompt 加在这里）
# Agent Memory：事实提取
# =============================================================================

FACTS_EXTRACT_PROMPT = """从以下对话中提取用户提到的关键事实和偏好。
只提取明确提到的、有信息量的内容。

要求：
- 每个事实是一条独立的陈述
- 只提取确定的信息，不要编造
- 用简洁的中文陈述
- 以 JSON 字符串数组格式返回

对话：
{conversation}

返回格式：
["事实1", "事实2", ...]"""


# Agent Memory：对话摘要
CONVERSATION_SUMMARY_PROMPT = """请用一句话总结以下对话的核心内容。

要求：
- 不超过 50 字
- 包含关键主题和结论
- 用中文

对话：
{conversation}"""



# =============================================================================
# 第 3 层：行为约束
#   具体任务的执行规则和输出格式要求
# =============================================================================

ENTITY_EXTRACT_PROMPT = """从以下文本中提取实体关系三元组 (实体, 关系, 实体)。

要求：
- 实体必须是具体的人名、地名、组织名、概念、技术名、产品名等
- 关系必须简洁明确（如"任职于"、"开发了"、"属于"、"位于"）
- 每个三元组单独一条，不要合并
- 只返回 JSON 数组，不要任何额外文字和 markdown 格式

文本：
{text}

返回格式：
[{{"subject": "实体1", "relation": "关系", "object": "实体2"}}]"""


TEXT2CYPHER_PROMPT = """你是一个 Neo4j 图数据库专家。基于以下图结构，将用户问题转为 Cypher 查询语句。

{schema}

要求：
- 只返回 Cypher 语句，不要任何额外文字
- 如果问题与图数据无关（不涉及实体关系），返回 "SKIP"
- 使用中文实体名作为查询条件
- 如需模糊匹配，使用 CONTAINS 或 STARTS WITH

用户问题：{question}

Cypher：
"""
